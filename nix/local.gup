#!bash -eu
dest="$2"
gup --always
gup -u ../root/systemd/nix
nix-build --out-link "$dest" default.nix -A siteInstalled --show-trace --option build-use-chroot false
nix-store --add --add-root --indirect "$dest"
