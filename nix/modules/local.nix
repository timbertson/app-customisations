{ pkgs, config, lib, ... }:
{
	# systemd.user.services.i2p = {
		# services.i2p = {
		# 	path = ["/usr"];
		# 	serviceConfig = {
		# 		ExecStart = "firejail --profile=i2p ${self.i2p}/bin/i2prouter-plain";
		# 		Restart="no";
		# 	};
		# };
	# };
}
