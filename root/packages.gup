#!bash -eu
set -x
pkcon install --allow-reinstall \
	gnome-tweak-tool \
	ecryptfs-utils \
	xautomation \
	xbindkeys \
	;

set +x

cat <<EOF
# === Suggested: ===
# pkcon remove setroubleshoot
# pkcon install tracker-preferences (and then disable tracker)
EOF
