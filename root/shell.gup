#!bash -eu
gup -u ../nix/local
FISH="$HOME/dev/app-customisations/nix/local/bin/fish"
if ! fgrep "$FISH" /etc/shells; then
	echo "$FISH" | sudo tee -a /etc/shells
fi

sudo usermod --shell "$FISH" tim
