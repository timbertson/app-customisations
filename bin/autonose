#!/bin/sh
if [ "$*" = "--versions" ]; then
  exec 0install run -gd 'http://gfxmonk.net/dist/0install/autonose.xml' "$@"
else
  exec 0install run  'http://gfxmonk.net/dist/0install/autonose.xml' "$@"
fi
