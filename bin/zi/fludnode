#!/bin/sh
if [ "$*" = "--versions" ]; then
  exec 0launch -gd 'http://gfxmonk.net/dist/0install/flud.xml' "$@"
else
  exec 0launch --main 'fludnode' 'http://gfxmonk.net/dist/0install/flud.xml' "$@"
fi
