#!/bin/sh
if [ "$*" = "--versions" ]; then
  exec 0launch -gd 'http://gfxmonk.net/dist/0install/supergenpass-platform.xml' "$@"
else
  exec 0launch  'http://gfxmonk.net/dist/0install/supergenpass-platform.xml' "$@"
fi
