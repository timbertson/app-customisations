#!/bin/sh
if [ "$#" -lt 2 ]; then
	echo "usage: mount.ssh [opts] remote local-dir"
	exit 2
fi

sshfs="0install run http://gfxmonk.net/dist/0install/sshfs.xml"
cmd="$sshfs"

if [ ! -e "$2" ]; then
	echo "Making directory: $2"
	mkdir -p "$2"
fi

$cmd "$@"
